language: python
python:
  # TODO: add tyiping support for older python versions
  # - "3.4"
  # - "3.5"
  - "3.6"

install:
  - pip install pytest-cov

script:
  - pytest --cov=src/ src/tests/ --cov-fail-under=100